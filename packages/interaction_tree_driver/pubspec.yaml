name: interaction_tree_driver
description: Flutter Driver integration for interaction_tree - custom finders and commands for automated testing
version: 0.1.0
homepage: https://github.com/rlch/interaction-tree

environment:
  sdk: ^3.10.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  interaction_tree:
    path: ../..

dev_dependencies:
  flutter_lints: ^5.0.0
