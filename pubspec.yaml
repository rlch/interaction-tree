name: interaction_tree
description: "Core package for interaction tree - annotate widgets with InteractionKey for automated testing"
version: 0.1.0
homepage: https://github.com/rlch/interaction-tree

environment:
  sdk: ^3.10.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
